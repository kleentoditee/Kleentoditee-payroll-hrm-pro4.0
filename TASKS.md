# Multi-Agent Task Board

Use this file as the live coordination board for Codex, Claude, Cursor, and the human owner.

## Current Integration Branch

- Branch: `codex/consolidate-live-build`
- Rule: finish locally, verify, commit, push.
- CodeRabbit: local CLI works through WSL; rerun after rate limits clear.

## Active Lanes

| Lane | Agent | Branch Pattern | Worktree Path | Owned Areas | Status |
| --- | --- | --- | --- | --- | --- |
| Integration QA | Codex | `agent/codex/integration-qa` | `C:\Kleentoditee Payroll HRM\codex-integration-qa` | docs, scripts, auth/shell polish, CodeRabbit fixes, final verification | Finance Tasks 9 + 10, tracker, and CodeRabbit fix pass pushed; CodeRabbit rerun waits on service rate limit |
| Finance Core | Claude | `agent/claude/finance-core` | `C:\Kleentoditee Payroll HRM\claude-finance-core` | finance API/UI, finance models, export/report logic | Tasks 9 + 10 merged; pull `codex/consolidate-live-build` before next slice |
| Employee Tracker | Cursor | `agent/cursor/employee-tracker` | `C:\Kleentoditee Payroll HRM\cursor-employee-tracker` | `apps/employee-tracker/**`, tracker UX, mobile employee flows | On `codex/consolidate-live-build`; pull to refresh worktree |

## Shared File Locks

Only one lane should edit these at a time. Add a row before touching a shared file.

| File | Locked By | Reason | Status |
| --- | --- | --- | --- |
| `packages/db/prisma/schema.prisma` | None | Shared data model | Free |
| `package.json` / `package-lock.json` | None | Dependencies/scripts | Free |
| `package.json` / `package-lock.json` | Cursor | Upgrade admin-web Next.js dependency | Done; admin-web typecheck/build passed |
| `apps/api/src/app.ts` | None | Route mounting | Free |
| `package.json` / `package-lock.json` | Kimi (main checkout) | Batch 6 R12: deepmerge-ts override attempt (reverted) | Done; both files back to pre-batch state |
| `packages/db/package.json` + `package-lock.json` + `packages/db/prisma/schema*.prisma` | Kimi (main checkout) | Batch 7 R12b: Prisma 7 upgrade | Done; Prisma 7.10.0 live-verified |
| `packages/db/prisma/schema.prisma` | Kimi (main checkout) | Batch 8 R7: general ledger models | Done; GL live-verified |
| `packages/db/prisma/schema.prisma` + `apps/api/src/app.ts` + `apps/api/src/lib/gl-posting.ts` + `apps/admin-web/src/lib/dashboard-nav.ts` | Kimi (main checkout) | Batch 17: migration-control schema + importer mount + OBE account + nav | Done; gate 34/34 live-verified |
| `packages/db/prisma/schema.prisma` | Kimi (main checkout) | Batch 18: disposition/migrationMode/exception-sign-off columns | Done; gate 45/45 live-verified |




## Lane Start Checklist

1. Run `git status -sb` in your worktree.
2. Confirm your branch and ownership in this file.
3. Pull/rebase from `codex/consolidate-live-build`.
4. Run `npm.cmd run db:generate` if schema changed recently.
5. Work only inside your owned areas unless you have a shared file lock.

## Lane Finish Checklist

1. Run focused checks for the lane.
2. Run workspace checks when the lane touches shared code:

```powershell
npm.cmd run typecheck
npm.cmd run lint
npm.cmd run test:api
```

3. Run CodeRabbit when available:

```powershell
wsl bash -lc "cd '/mnt/c/Kleentoditee Payroll HRM/Kleentoditee-payroll-hrm-pro4.0' && coderabbit review --agent -t uncommitted -c .coderabbit.yaml"
```

4. Commit with a clear message.
5. Push your branch.
6. Update this board with what changed, what passed, and what is next.

## Suggested Next Parallel Slices

| Slice | Best Agent | Why |
| --- | --- | --- |
| Paystubs and export polish (no banking) | Claude | User priority is payroll output and exports, not banking workflows |
| Admin/tracker readability fixes | Cursor | Left rail widgets and top action buttons need larger, clearer UI |
| Cross-app QA, CodeRabbit pass, and merge cleanup | Codex | Integration and verification lane |


---

## Kimi Lane — Release-Truth Batch (2026-09-18)

- Agent: Kimi (owner-directed session)
- Branch: working in main checkout `cleanup/project-workflow-audit` (owner has large uncommitted WIP; no branch switch, no commit sweep)
- Basis: `docs/DEEP-RESEARCH-2026-09-18.md` Batch 1
- Owned files this batch:
  - `apps/admin-web/src/lib/dashboard-nav.ts`
  - `apps/admin-web/src/components/app-shell.tsx`
  - `packages/db/prisma/schema.prisma` (shared — lock below)
  - `packages/db/prisma/seed.ts`
  - `apps/api/src/routes/settings.ts`
  - `apps/admin-web/src/app/dashboard/settings/page.tsx`
  - `apps/admin-web/src/app/dashboard/payroll/runs/page.tsx` (warning banner only, if touched)
- Shared File Locks:
  - `packages/db/prisma/schema.prisma` | Kimi | Add StatutoryRateVersion model | Active
- Status: nav fixes done; statutory versioning in progress

### Batch 1 results (2026-09-18)

- Navigation fixes shipped in working tree:
  - "Paystubs" now points to `/dashboard/payroll/paystubs/preview` (was pay runs list); "Payroll exports" label removed; "Government forms" added under Payroll; primary-nav "Bookmarks" removed; duplicate "Users & roles" (People group) and "Activity log" (Admin group) removed.
  - Payroll section tab bar (`payroll/layout.tsx`) now includes Paystubs alongside Pay periods / Pay runs / Government forms.
- Statutory versioning:
  - New `StatutoryRateVersion` model in `schema.prisma` (pushed to local PostgreSQL, Prisma client regenerated). Payroll still reads OrgSettings; this is the verification/audit table.
  - `GET /settings/org` now returns `statutoryVerification` (hasVersion / verified / approved / source / dates).
  - Seed adds a current-year UNVERIFIED version row (only when seed runs; not re-seeded this session to avoid resetting owner data).
  - Settings page shows a dynamic banner: green when verified+approved, amber warning otherwise. Pay runs page shows an amber warning with a Settings link until rates are verified and approved.
- Verification: typecheck PASS, lint PASS (1 pre-existing `<img>` warning in `payroll/forms/page.tsx`), test:api 72/72 PASS. Browser-verified in-app: settings banner, pay runs banner, 4-tab payroll nav, government forms page all render correctly.
- Notes: API process was restarted to unblock Prisma generate (EPERM on query engine DLL). No commit made — main checkout has extensive owner WIP; integration lane should commit deliberately.
- Next batch: Batch 2 = cookie-session auth + CSRF + DB-backed throttle (research R3/R4).

| `apps/api/package.json` | Kimi | Add auth-cookies.test.ts to API test list | Active |

### Batch 2 results (2026-09-19) — cookie sessions + CSRF + DB throttle

- Security model changed from `sessionStorage` bearer tokens to server-managed cookies:
  - Sign-in (login / first-user register / dev-emergency) now sets `kt_session` (HttpOnly, SameSite=Lax, Secure in production, 12h) + `kt_csrf` (readable double-submit token). New files: `apps/api/src/lib/auth-cookies.ts`, `apps/api/src/lib/rate-limit.ts`; middleware rewritten in `apps/api/src/middleware/auth.ts`.
  - CSRF enforced on all cookie-authenticated mutations: `x-kt-csrf` header must match the `kt_csrf` cookie (constant-time compare). Bearer-auth requests are exempt (transitional compatibility) — bearer tokens are still returned in login responses for scripts/migration; removal is a later batch.
  - New `POST /auth/logout` revokes the session server-side (bumps `tokenVersion`, expires both cookies); admin shell and tracker home call it on sign-out.
  - Login throttling + password-reset interval throttle moved from in-memory Maps to new `AuthRateLimit` Prisma table (survives restarts, multi-process safe).
  - Frontends: `setToken` no longer stores tokens; a global fetch guard (`installCsrfFetchGuard` in both `lib/api.ts`, bootstrapped by new `components/csrf-guard.tsx` in both root layouts) attaches the CSRF header to API mutations automatically. `getToken()` returns a sentinel for legacy page guards while the cookie exists.
  - Protected layouts are cookie-first (admin `dashboard/layout.tsx`, tracker home): no stored token required; 401 from `/auth/me` redirects to login.
- Schema: added `AuthRateLimit` model (db push + generate done).
- Verification: typecheck PASS; lint PASS (1 pre-existing `<img>` warning); test:api **81/81 PASS** (9 new cookie/CSRF tests in `auth-cookies.test.ts`, added to the api test script); full `build` PASS (admin, tracker, api); `check:tracker-login` PASS.
  - curl-level proof: cookie GET 200; cookie POST without CSRF header → 403; with header → 200; after logout → 401; bearer fallback → 200.
  - Browser proof (in-app): admin sign-in leaves storage empty, `kt_session` invisible to scripts, settings save (PUT) succeeds through the guard; tracker (Maria) Staff Hub loads cookie-only.
- Data note: `maria.tracker@kleentoditee.local` user was **suspended** in the local DB (pre-existing state, created 2026-09-17). Reactivated via `/admin/users/:id/reactivate` to verify the tracker flow — flag to owner if that suspension was intentional.
- Still open (later batches): MFA, session/device management UI, remove bearer fallback, security headers review, `npm audit` triage.

### Batch 3 results (2026-09-19) — historical/YTD payroll opening-balance import (R5)

- New `PayrollYtdOpeningBalance` model (`packages/db/prisma/schema.prisma`): one row per employee per calendar year (`@@unique([employeeId, year])`) holding opening gross + NHI/SSB/income-tax/payroll-tax (employee + employer) + net, with source/notes/importedBy audit fields. Pushed to local PostgreSQL; Prisma client regenerated.
- Payroll-tax correctness fix: `payroll-service.ts` now merges opening-balance gross into `yearToDateGrossByEmployee` before building run items, so the BVI annual exemption ($10,000 default) accounts for pay earned under a previous system. Unit-proven: $2,000 fixed month yields $0 payroll tax without history vs $80 with a $9,000 opening balance (8% of the $1,000 over the exemption).
- New lib `apps/api/src/lib/payroll-ytd-import.ts`: quote-aware CSV parser (fixed template: email, employee_name, gross, nhi, ssb, income_tax, payroll_tax, employer_nhi, employer_ssb, employer_payroll_tax, net, notes), validation (non-negative amounts, gross required), employee matching (case-insensitive email, exact-name fallback, ambiguous names rejected), duplicate-employee detection per file, all-or-nothing upsert commit.
- Routes in `apps/api/src/routes/payroll.ts` (no app.ts change): `GET /payroll/ytd-opening-balances?year=` (CAN_VIEW), `POST .../preview` and `.../commit` (CAN_EDIT), `DELETE .../:id` (CAN_EDIT). Commits and deletes write audit log entries.
- Admin UI: new `/dashboard/payroll/ytd-import` page (year picker, CSV template download, file/paste input, preview table with New/Overwrites badges and row errors, commit, recorded-balances table with delete, employees-without-balance hint). Added "YTD import" tab to `payroll/layout.tsx` and sidebar entry in `dashboard-nav.ts`.
- Verification: typecheck PASS; lint PASS (same 1 pre-existing `<img>` warning); test:api **91/91 PASS** (10 new in `payroll-ytd-import.test.ts`, added to api test script); full `build` PASS.
  - curl proof: bad-row commit 400; CSRF-less commit 403; clean commit created/updated counts correct; duplicate-row file 400; delete 200 then 404; list reflects state.
  - Browser proof (in-app): sign-in, 5-tab payroll nav, preview (1 ready / 0 rejected, matched employee, formatted amounts), commit success banner, recorded table row, UI delete → clean state. Test rows fully removed afterwards; owner data untouched (0 balances remain).
- Ops note: killing the API to unblock Prisma generate also stopped the shared `dev-all` process group; API, admin, and tracker were all restarted and confirmed healthy (8787/3000/3001). Restart helpers saved at `tmp/restart-api.ps1` and `tmp/restart-frontends.ps1`.
- Next batch: Batch 4 = payroll register + reconciliation reports (R9) — the opening-balance statutory components stored here feed those reports.

### Batch 4 results (2026-09-19) — payroll register + year summary + reconciliation (R9)

- New lib `apps/api/src/lib/payroll-reports.ts` (pure aggregation separated from DB loading):
  - **Payroll register** per run: frozen per-employee lines (days/hours/OT, gross, NHI/SSB/income-tax/payroll-tax, manual + total deductions, net, employer NHI/SSB/payroll-tax + employer cost), totals row, CSV export with period header block and quoted-field escaping. Void runs rejected; drafts allowed but flagged.
  - **Year summary** per calendar year: per-employee Opening YTD (Batch 3 balances) + posted-runs gross = YTD gross, plus all deduction/employer components, grand totals, posted-run list; run lines belonging to deleted employees are kept as inactive rows rather than dropped.
  - **Reconciliation checks** on every frozen line: `gross = net + totalDeductions`, `totalDeductions = nhi+ssb+incomeTax+payrollTax+manual`, non-negative net, and per-employee `opening + runs = YTD gross`; draft/void runs in the year surface as exclusion warnings.
- Routes in `payroll.ts` (CAN_VIEW): `GET /payroll/reports/register?runId=&format=csv` (CSV download via Content-Disposition) and `GET /payroll/reports/year-summary?year=`.
- Admin UI: new `/dashboard/payroll/reports` page — run picker + register table + CSV download + draft banner; year picker + per-employee YTD table (opening vs runs vs YTD columns) + posted-run listing; green/amber/rose reconciliation banners. Added "Reports" tab (6th) to `payroll/layout.tsx` and sidebar entry in `dashboard-nav.ts`.
- Verification: typecheck PASS; lint PASS (same 1 pre-existing `<img>` warning); test:api **98/98 PASS** (7 new in `payroll-reports.test.ts`); full `build` PASS.
  - curl proof on a labeled draft run over the owner's September period: register JSON reconciles (gross 6800 = net 6295 + deductions 505), CSV downloads with correct headers/filename, year summary excludes the draft with a warning and reports zero posted activity.
  - Browser proof (in-app): page renders register lines + totals, reconciliation-passed banner, year summary with draft-exclusion warning.
  - Cleanup: draft run deleted afterwards; owner data restored (0 runs, 2 original periods, 0 opening balances). Only audit-log entries remain from the verification.
- No commit made — owner WIP in checkout; integration lane commits deliberately.
- Still open from research: R6 document storage hosting, R7 finance ledger scope, R8 leave/time-off, R10 scheduling, R11 ops, R12 npm audit + Prisma 7 deprecation, R13 e2e/visual tests.
### Batch 5 results (2026-09-19) — leave/time-off domain (R8)

- Schema (`packages/db/prisma/schema.prisma`, pushed + client regenerated): new `UNPAID_LEAVE` value on `StaffRequestType`; new `LeavePolicy` model (`code`/`requestType` unique, `paid`, `annualAllowanceDays` with 0 = untracked, `active`, `sortOrder`); `PayRunItem` gained `unpaidLeaveDays` + `unpaidLeaveDeduction` so the pay-run line freezes the leave impact.
- New pure lib `apps/api/src/lib/leave-days.ts`: business-day counting, range overlap, and fixed-basis pro-rata deduction (`fixedPay x days / standardDays`, period-business-days fallback, capped at fixedPay). `apps/api/src/lib/leave.ts` adds `DEFAULT_LEAVE_POLICIES` (ANNUAL 15d paid, SICK 10d paid, UNPAID untracked unpaid), `computeLeaveBalances` (used = APPROVED+COMPLETED, pending = SUBMITTED+UNDER_REVIEW), `ensureDefaultLeavePolicies`, `listLeaveBalances`, `loadUnpaidLeaveDaysByEmployee`, `isLeaveRequestType`.
- Payroll integration: `payroll-run-builder.ts` accepts `unpaidLeaveDaysByEmployee` + `standardDays`, routes `fixedPay - deduction` through `computeEmployeeRunLine` so gross AND statutory (NHI/SSB) both drop; `payroll-service.ts` loads approved unpaid-leave overlap per period automatically. Unit-proven: $2,200 fixed with 11/22 unpaid days -> $1,100 gross and NHI follows the reduced gross.
- Routes in `apps/api/src/routes/staff-requests.ts` (mounted at `/`, no app.ts change): `GET /leave/policies`, `POST /leave/policies/seed-defaults`, `PUT /leave/policies/:id` (owner/hr), `GET /leave/balances?year=&employeeId=`, `GET /staff/self/leave-balances?year=`. `UNPAID_LEAVE` wired into request types, payroll visibility, and date-required validation. `seed.ts` upserts the 3 default policies.
- Admin UI: new `/dashboard/people/leave` page (editable policy table + per-employee balances grid with used/pending/remaining), "Leave balances" tab in `people/layout.tsx` + sidebar entry. Tracker UI: `UNPAID_LEAVE` type + label, date-range handling, "My leave balances" card on `/requests`.
- Bug found + fixed during browser verification (Batch 2 follow-up): `hasSessionCookie()` in both `apps/*/src/lib/auth-storage.ts` checked `document.cookie` for the **HttpOnly** `kt_session` cookie, which scripts can never see -- every tracker page guard bounced a signed-in employee back to /login. Fixed to detect the readable `kt_csrf` companion cookie (set/expired together with `kt_session`, same TTL). tsc + eslint PASS on both apps; admin was unaffected (its layout guards via `/auth/me`).
- Verification: typecheck PASS; lint PASS (same 1 pre-existing `<img>` warning); test:api **103/103 PASS** (5 new in `leave.test.ts`); full `build` PASS.
  - curl proof: policies seeded; tracker login as Maria; UNPAID_LEAVE (2026-09-07..11) + TIME_OFF submitted, approved via admin PATCH; self-balances flipped pending -> used (5 unpaid, 3 annual/12 remaining); admin balances list all 4 employees.
  - Payroll impact proven live on the owner's September period with a labeled approved UNPAID_LEAVE for Kemario (fixed $1,800, standardDays 20): draft run line showed `unpaidLeaveDays=5`, `unpaidLeaveDeduction=450`, `gross=1350`, `net 1245.37 + deductions 104.63 = gross` reconciled; register report lines reconcile for all employees.
  - Browser proof (in-app): admin `/dashboard/people/leave` renders policies table + 0-used balances grid + People tab bar; tracker `/requests` renders "My leave balances" card (15/10 left, 0d unpaid used) and the Unpaid leave type option.
  - Cleanup: draft run deleted (runs list empty), all 3 test staff requests deleted (table count 0), balances verified all-zero afterwards; the 3 seeded leave policies intentionally remain (additive feature data, same pattern as Batch 1 StatutoryRateVersion). Temp jars/scripts removed.
- No commit made -- owner WIP in checkout; integration lane commits deliberately.
- Still open from research: R6 document storage hosting, R7 finance ledger scope, R10 scheduling, R11 ops, R12 npm audit + Prisma 7 deprecation, R13 e2e/visual tests.
### Batch 6 results (2026-09-19) — npm audit + Prisma deprecation triage (R12)

- Audit baseline (npm 11.12.1, `npm audit --json`): **3 high, 0 critical/moderate/low** — a single transitive chain: `deepmerge-ts@7.1.5` (GHSA-ggr8-5vv4-36mx, stack exhaustion merging recursive object graphs) <- `@prisma/config@6.19.3` <- `prisma@6.19.3` (CLI, devDependency of `packages/db`).
- Exposure analysis: the vulnerable code runs only inside `loadConfigTsOrJs` when the Prisma CLI parses a `prisma.config.ts` file; this repo has none (config lives in `packages/db/package.json`), so the path is never exercised. Dev-time CLI only; the runtime `@prisma/client` is unaffected. Practical risk: low.
- No fix on the Prisma 6 line: `prisma@6.19.3` is the latest 6.x and pins `deepmerge-ts` exactly `7.1.5`. Fixed only in Prisma 7+ (`prisma@7.10.0` current stable; `@prisma/config@7.10.0` no longer depends on deepmerge-ts). Prisma 8 is already in RC (`8.0.0-rc.15`).
- Deprecation status: `prisma@6.19.3` carries NO npm `deprecated` flag. The "Prisma 7" item is an upgrade-window concern, not an npm deprecation.
- Workaround attempted and reverted: npm `overrides` for deepmerge-ts (scoped `@prisma/config` + flat `^8.0.0` + exact `8.0.0`, with lock/hidden-lock/forced re-resolution). npm 11.12.1 silently DROPS the package from the ideal tree when the override conflicts with an exact pin — `node_modules/deepmerge-ts` disappears and `prisma generate` crashes. Resolver bug, not fixable by override syntax. All changes fully reverted; lock restored byte-identical to pre-batch state (git-clean vs HEAD), `package.json` carries only the owner's pre-existing WIP.
- Post-revert verification: `prisma generate` + `prisma validate` PASS; test:api **103/103 PASS**; api typecheck PASS; audit back to the 3-high baseline (no regression); API healthy on 8787.
- No commit made — owner WIP in checkout; integration lane commits deliberately.
- Outcome / recommendation: accept the 3 dev-time highs for now (documented, unused code path); schedule a dedicated **Prisma 7 migration batch** — expected scope per the v7 upgrade guide (confirm against the official guide at implementation time): new `prisma-client` generator output, required `prisma.config.ts`, seed config move out of package.json, driver-adapter client (`@prisma/adapter-pg`), plus full test/build/live re-verification.
- Still open from research: R6 document storage hosting, R7 finance ledger scope, R10 scheduling, R11 ops, R13 e2e/visual tests. New: **R12b Prisma 7 migration** (from this triage).
### Batch 7 results (2026-09-19) — Prisma 7 upgrade (R12b, from Batch 6 triage)

- Upgraded `prisma` + `@prisma/client` 6.19.3 -> **7.10.0** in `packages/db`, added `@prisma/adapter-pg@7.10.0` (v7 client is Rust-free and requires a driver adapter even with the legacy provider).
- Kept the `prisma-client-js` generator (still supported in v7; the new `prisma-client` provider + generated-output-path import migration deferred — not required for the audit fix and a much larger blast radius).
- Schema: `datasource.url` is REMOVED in v7 (P1012, not just deprecated) — dropped `url = env("DATABASE_URL")` from `schema.prisma` and `schema.sqlite.prisma`. SQLite fallback validated (`prisma validate --schema prisma/schema.sqlite.prisma` PASS).
- New `packages/db/prisma.config.ts` (loads root `.env` via dotenv, registers schema path, migrations path, `tsx prisma/seed.ts` seed, datasource url). Removed the now-ignored `"prisma"` key from `packages/db/package.json` and dropped the removed `--skip-generate` flag from `push`/`push:loss` scripts.
- Runtime instantiation: `packages/db/src/index.ts` now builds `new PrismaClient({ adapter: new PrismaPg({ connectionString }) })` behind a **lazy Proxy singleton** — v6 constructed eagerly without env, but v7 needs DATABASE_URL at construction, and several unit tests import the module for types/enums only. Cache lives on `globalThis` under `prismaClient` (the proxy itself must never be cached — that bug caused a stack-overflow 500 during verification and was fixed). Same adapter pattern applied to `scripts/reset-password.mjs`.
- Verification: `prisma generate` + `validate` PASS; `db push` = database already in sync (no structural drift); test:api **103/103 PASS**; db + api typecheck PASS; full workspace `build` PASS; lint PASS (same 1 pre-existing `<img>` warning).
  - Live proof: API restarted on 8787; admin login sets kt_session/kt_csrf cookies; leave policies, balances (4 rows), payroll periods, auth/me all return live data through the pg adapter; draft-run create/delete on the September period reconciles (Kemario gross back to full 1800, confirming Batch 5 cleanup) and was removed afterwards.
  - Browser proof (in-app): admin `/dashboard/people/leave` renders policies + balances grid on the Prisma 7 stack. Note: the in-app browser shares cookies across localhost ports, so the tracker login from Batch 5 initially shadowed the admin session (cookies ignore ports) — re-signed in as admin; dev-only quirk, not a regression.
- Audit outcome: the deepmerge-ts chain is NOT cleared — `@prisma/config@7.10.0` still pins `deepmerge-ts@7.1.5`, and v7 additionally bundles `mysql2@3.15.3` (2 new advisories: auth-plugin downgrade, zlib decompression bomb). Current: **4 highs**, all inside the dev-time Prisma CLI. No stable release fixes them (ranges end in the 8.1.0-dev channel; prisma 8.0.0-rc is a full CLI rewrite and not production-appropriate). Exposure: deepmerge-ts runs only when the CLI parses our own static prisma.config.ts; mysql2 is dead code for a PostgreSQL deployment. Accept + monitor: re-run `npm audit` each batch and bump prisma when a stable release carrying deepmerge-ts >= 8 / mysql2 > 3.23.0 ships.
- No commit made — owner WIP in checkout; integration lane commits deliberately. Changed files: `packages/db/package.json`, `packages/db/prisma.config.ts` (new), `packages/db/src/index.ts`, `packages/db/prisma/schema.prisma`, `packages/db/prisma/schema.sqlite.prisma`, `scripts/reset-password.mjs`, `package-lock.json`.
- Ops note: seed.ts wipes tables before seeding — never run `npm run db:seed` / `prisma db seed` against the live owner DB.
- Still open from research: R6 document storage hosting, R7 finance ledger scope, R10 scheduling, R11 ops, R13 e2e/visual tests. Follow-up: prisma-client-js -> prisma-client provider migration when Prisma announces the removal release.
### Batch 8 results (2026-09-19) — double-entry general ledger core (R7, first slice)

- Schema (`packages/db/prisma/schema.prisma`, pushed): `JournalEntry` (date, memo, `sourceType`/`sourceId`, unique `sourceKey` for idempotency, `status`, `reversalOfId` self-relation, `createdByUserId`) + `JournalLine` (`Decimal(14,2)` debit/credit — decimal-safe storage, unlike the Float documents) + `JournalEntryStatus` enum; `Account.journalLines` relation. Posting rule: corrections are reversal journals (`<type>_reversal` sourceType), posted rows are never edited or deleted by the app.
- New lib `apps/api/src/lib/gl-posting.ts`: pure validators/builders (2+-line rule, one-sided debit/credit, balance within half-cent) + `postJournal` (idempotent upsert by sourceKey with unique-race fallback) + `reverseJournal` (swap debit/credit, no-op when the original predates the GL) + `ensureControlAccounts` (additive upsert-by-code, same pattern as leave policies). Control accounts: AR 1100 / AP 2000 (already seeded) + new 2100 NHI, 2200 SSB, 2300 payroll-tax, 2400 income-tax, 2500 net-wages, 2600 other-deductions payables, 2700 tax payable, 6100 wages, 6200 employer statutory. `seed.ts` upserts the same set for fresh installs.
- Posting rules wired into existing transactions (same DB transaction as the document mutation): invoice send (Dr AR / Cr income per line / Cr tax), invoice void (reversal), customer payment create (Dr deposit account / Cr AR) + delete (reversal), bill receive (Dr expense per line / Cr AP) + void (reversal), bill payment create (Dr AP / Cr source account) + delete (reversal), expense post (Dr expense / Cr payment account) + void (reversal), payroll run finalize (Dr wages gross + Dr employer statutory / Cr NHI+SSB+payroll-tax+income-tax+other-deductions payables + Cr net wages) + void (reversal). Deposits deliberately NOT auto-posted: deposit lines carry no account attribution and payments already post at receipt — documented for a later slice.
- New lib `apps/api/src/lib/gl-reports.ts` + endpoints in the already-mounted `finance-reports.ts` (CAN_VIEW, no app.ts change): `GET /finance/reports/trial-balance?asOf=`, `GET /finance/reports/journal?from=&to=&sourceType=`, `GET /finance/reports/ledger/:accountId?from=&to=` (opening balance + running normal-direction balance).
- Admin UI: `/dashboard/finance/journal` (source filter, expandable line drill-down, reversal badges) + `/dashboard/finance/trial-balance` (as-of picker, balanced banner, normal-direction balances, "management-prepared, unaudited" label) + 2 new finance tabs.
- Verification: test:api **111/111 PASS** (8 new in `gl-posting.test.ts`, incl. the Batch 5 payroll numbers as a balanced-journal proof); db+api+admin typecheck PASS; eslint on new pages PASS; full workspace build PASS.
  - Live proof: invoice→send→payment→bill→receive→bill-payment→expense→post chain produced 5 balanced journals; second invoice send→void produced an exact reversal; trial balance after the flow: balanced 2260=2260 with Cash 90 / AR 600 / Sales 1000 / COGS 250 / Office 60 (hand-verified); September draft run finalized → payroll journal dr 7336.25 = cr 7336.25 with auto-provisioned control accounts → voided → exact reversal, all payroll accounts back to 0.
  - Browser proof (in-app): journal page (tabs, filter, empty state) and trial-balance page (balanced banner, unaudited label) render live.
  - Cleanup: payment/bill-payment deleted via API (reversals posted), invoice/bill/expense voided via API (reversals) then all test documents + 14 journal entries DB-deleted; voided test pay run DB-deleted. Final state: 0 journal entries, TB 0 rows, owner's original INV-2026-0001/BILL-2026-0001 untouched, 15-account chart (6 original + 9 control) kept as feature data.
- No commit made — owner WIP in checkout; integration lane commits deliberately.
- R7 remaining slices (documented for next batches): fiscal periods with open/locked states, manual journals with approval, opening balances/conversion, AR/AP aging off journal lines, deposits posting design (undeposited-funds account), P&L / balance sheet / cash-flow reports, CSV/PDF export, year-end closing + retained earnings.
- Still open from research: R6 document storage hosting, R10 scheduling, R11 ops, R13 e2e/visual tests.
### Batch 9 results (2026-09-19) — deep review & gap analysis (self-audit)
- Full audit executed against the live working tree; report written to `docs/DEEP-REVIEW-2026-09-19.md`. A copy-paste ChatGPT version of the review brief lives at `docs/CHATGPT-DEEP-REVIEW-PROMPT.md`.
- Build health re-verified this session: full workspace build PASS, typecheck PASS, test:api 111/111 PASS, lint PASS (0 errors / 1 warning), npm audit unchanged (4 high, dev-CLI prisma chain only).
- Batch 1-8 claims re-verified against code: all VERIFIED, none false. Deposits-posting gap confirmed real (zero postJournal refs in finance-deposits.ts).
- Critical findings: (1) ALL of Batches 1-8 are uncommitted working-tree state (214 modified + 86 untracked files, zero commits) — highest-risk item; (2) branch `frontend/auth-client-cookie-ready` (56feb29) is merged nowhere — diff before deleting; (3) seeded DeductionTemplate "NHI + SSB + income tax" mislabels BVI payroll tax as income tax in a zero-income-tax jurisdiction; (4) SSB/NHI seed ceilings mismatch public sources and provenance fields are empty; payrollTaxEmployerClass is NOT_SET; (5) sick-leave seed 10d vs cited 12-day statutory minimum — verify Labour Code.
- K3 gap map: multi-tenancy NOT STARTED (zero tenant fields in schema), banking/reconciliation NOT STARTED, financial statements + year-end package + BVI filing outputs NOT STARTED, WhatsApp still on recipient-less api.whatsapp.com link, email invite/reset-only, S3/R2 storage unimplemented, no e2e framework anywhere.
- Competitor verdict (BVI-weighted): QBO/Xero/Gusto/BambooHR/Wave all lack BVI payroll — the app's BVI payroll + integrated ledger is the differentiator; real regional competitor is Celery. Behind everyone on banking, statements, mobile, hardening.
- Recommended next: Batch 10 = commit the verified baseline (K3 commit sequence), Batch 11 = statutory truth pack, Batch 12 = GL slice 2 (fiscal periods + manual journals + deposits posting).
- No commit made — owner WIP in checkout; integration lane commits deliberately.
- Reconciliation (2026-09-19): owner ran the same brief in ChatGPT; comparison written to `docs/DEEP-REVIEW-2026-09-19-RECONCILIATION.md` (file: `docs/DEEP-REVIEW-RECONCILIATION-2026-09-19.md`). ChatGPT confirmed 5 additional findings Kimi verified true: missing migrations for Batches 1/2/3/5/8 models (deploy blocker), CORS missing x-kt-csrf + credentials, bearer fallback still active, 107 Float money fields, BZD hardcoded on customer pages + GL account 2400 "Income Tax Withheld Payable". Kimi's NHI-ceiling flag was wrong (official 2026 bulletin confirms 106,800) and is corrected in the report. Agreed order: Batch 10 = reproducible baseline (commits + migrations), Batch 11 = statutory/jurisdiction truth pack + CORS/bearer decision, Batch 12 = accounting integrity slice 2, Batch 13 = multi-tenancy track (before further GL slices), then statements/banking/filing/comms/e2e.
### Master shipping plan adopted (2026-09-19)
- `docs/SHIPPING-PLAN.md` is now the ACTIVE plan, superseding older batch lists. It merges the K3 baseline, both deep reviews, the reconciliation, and the GPT shipping conclusion, renumbered to this board's history (next = Batch 10).
- Owner directive recorded: SSB ceiling verification is NOT required; the configured value is accepted and removed from all gates.
- Two releases: Gate A internal pilot after Batch 11; Gate B subscriber SaaS after Batch 16. Order: B10 baseline (commits+migrations+CORS/bearer) -> B11 payroll truth/jurisdiction cleanup (Gate A) -> B12 multi-tenancy -> B13 accounting integrity -> B14 banking -> B15 statements+BVI filing -> B16 storage/email/WhatsApp/e2e (Gate B).
### Batch 10 results (2026-09-19) — reproducible baseline: commits + migrations + auth deploy contract
- Dirty worktree classified (219 paths) and committed in 8 coherent chunks, pushed to origin `cleanup/project-workflow-audit`: 671944b chore(repo), 4a8384c docs, 377c9ef feat(db), f476dc6 feat(api), bcd5f66 feat(admin-web), de25cd7 feat(employee-tracker), 5912a75 chore(scripts), 59af3c1 chore(ops). Worktree clean.
- Stranded branch `frontend/auth-client-cookie-ready` (56feb29) diffed: current working tree is a strict evolution (same same-origin proxy pattern + CSRF injection the branch lacked). NOTHING to salvage; branch superseded, safe to archive.
- Missing-migration blocker fixed: new migration `20260919230000_release_truth_batches` covers StatutoryRateVersion, AuthRateLimit, PayrollYtdOpeningBalance, LeavePolicy + PayRun/PayRunItem leave fields, JournalEntry/JournalLine, PayRunStatus.void, StaffRequestType.UNPAID_LEAVE.
- Proof method (no docker on this machine; native PostgreSQL 18 where agent-spawned servers fail 0xC0000142): replayed all 8 existing migrations in single-user scratch postgres, applied candidate, hash-compared catalog vs live DB — 537 columns identical, 97 enum labels identical, zero replay errors. Live DB baselined with `migrate resolve --applied` x9; `migrate status` = up to date. Zero drift live<->schema confirmed before baseline. NOTE: full `migrate deploy` through prisma on an empty DB remains unexecuted locally (no second server possible) — run once in CI/staging; the SQL itself is replay-proven.
- CORS deployment contract fixed in `apps/api/src/app.ts`: allowHeaders now includes `x-kt-csrf`, `credentials: true`. Bearer-token fallback KEPT deliberately (used by ops scripts smoke-finance-*.mjs + check-tracker-login.mjs; browsers use cookies only) — documented in code.
- Surprises found: tracked HEAD schema was still the old SQLITE schema; entire migrations/ dir was untracked; live DB is native PostgreSQL 18 (not docker); docker-compose.yml + LOCAL_DEVELOPMENT_WINDOWS.md docker instructions are stale for this machine.
- Verification after changes: api tsc PASS, test:api 111/111 PASS, cold-start login 200 + /auth/me 200 + trial-balance 200, worktree clean, pushed.
- Next batch: Batch 11 — BVI payroll truth + jurisdiction cleanup (Gate A). See docs/SHIPPING-PLAN.md.
### Batch 11 results (2026-09-20) — BVI payroll truth + jurisdiction cleanup (Gate A code complete)

- Version-driven payroll: `loadOrgPayrollContext(schedule, asOf)` now resolves an approved `StatutoryRateVersion` for the period year FIRST (with `statutorySource` provenance meta), falling back to OrgSettings. `finalizeRun` refuses an unapproved version and freezes `statutorySnapshot` {config, source, frozenAt} on the run — every finalized run now carries an auditable provenance record. Live DB: inserted + approved `srv_2026_bvi` (2026: SSB 4%/4.5% ceiling 53,400; NHI 3.75%/3.75% ceiling 106,800; payroll tax 8% CLASS_1, exemption 10,000) with sourceUrl + verifiedBy/approvedBy per owner directive.
- Full payroll money lifecycle in GL: finalize posts accrual (Dr 6100 wages + Dr 6200 employer statutory / Cr 2100 NHI + 2200 SSB + 2300 payroll-tax + 2500 net wages); NEW `markRunPaid` posts settlement journal (Dr 2500 / Cr 1000 cash, sourceType `payroll_run_paid`) in the same tx; NEW `POST /payroll/runs/:id/remit-statutory` (service `remitRunStatutory`) posts remittance (Dr 2100/2200/2300 totals / Cr 1000, sourceType `payroll_statutory_remittance`, guarded vs draft/void/already-remitted, `PayRun.statutoryRemittedAt` recorded); `voidRun` now reverses all three journals (requires `reversePaid: true` confirmation when paid). Schema + migration `20260920001000_payroll_statutory_remittance` applied live and resolved.
- Income-tax residue removed (BVI has no income tax): GL control account 2400 + always-zero incomeTax journal line deleted from `gl-posting.ts`; live account 2400 deleted (0 journal lines); seed chart cleaned; misleading deduction template renamed live + in seed to "NHI + SSB (full statutory)" (its 8% "income tax" was never applied — calc forces incomeTax off).
- Jurisdiction cleanup: BZD→USD (en-VI) on finance customer pages; remaining "Belize" strings are test-fixture names only. SICK leave policy bumped 10→12 days live + in seed (BVI Labour Code minimum).
- Minimum-wage validation: new `apps/api/src/lib/min-wage.ts` (BVI $7.25/hr eff 2025-07-01; hourly/daily÷8/fixed×periods÷2080 effective rate) wired as additive `minimumWageWarning` into employee create/detail/update responses — advisory only, never blocks.
- Gate A parallel-run artifact: `payroll-parallel.test.ts` — 13 tests comparing `computeEmployeeRunLine` against an independent reference implementation across monthly/weekly/biweekly, ceiling boundaries, exemption crossing, Class 1/2, hourly+OT+bonus, daily multi-line, manual deductions.
- Live verification (2 full cycles on TEST periods, all cleaned up): version-driven snapshot frozen with CLASS_1 + correct ceilings; journals balanced (accrual 4,654.75 = 4,654.75 hand-verified); settlement Dr 2500/Cr 1000; remittance cleared 2100+2200 against 1000; payroll tax correctly ZERO in May (cumulative gross below $10k annual exemption — engine YTD logic confirmed correct); guards hold (remit twice → 400, void paid run without reversePaid → 400); void with reversePaid posted exact reversals of all 3 journals with every account netting to 0. Post-cleanup: 0 test rows remain, owner data untouched.
- Verification: api tsc PASS, test:api **129/129 PASS** (+18 new: 5 min-wage, 13 parallel), workspace typecheck PASS, lint PASS (1 pre-existing warning), full build PASS.
- Gate A remaining (non-code): backup/restore drill, owner sign-off, one monitored parallel live run on real September data. Browser render of the runs/[id] remittance button not re-checked visually (typecheck/build only).
- Next batch: Batch 12 — multi-tenancy foundation. See docs/SHIPPING-PLAN.md.
### Batch 12 results (2026-09-20) — multi-tenant security foundation (GATE PASSED)

- Schema: new `Organization` (slug, status active/suspended) + `OrganizationMembership` (@@unique([orgId,userId]), cascade FKs); `User.isPlatformSupport`; `orgId` on all 36 business models (AuditLog nullable for org-less auth events); org-aware composite uniques for account codes, customer/supplier displayName, product SKU, invoice/bill/payment/bill-payment/expense/deposit numbers, paystub numbers, GL sourceKey, leave-policy code/requestType, pay-period window, statutory-version year; `OrgSettings` is now one row per org (`orgId @unique`, singleton pattern removed).
- Migration `20260920093000_multi_tenant_foundation`: hand-built from prisma diff + lossless backfill — default org `org_kleentoditee`, memberships for all existing users, every existing row assigned in one transaction with pre-commit count checks; applied live, `migrate resolve --applied`, status clean.
- Scoping architecture (packages/db): `AsyncLocalStorage` org scope installed by the API auth middleware; the exported `prisma` proxy resolves to a per-org `$extends` client that rewrites every operation on tenant models — orgId merged into all where clauses (incl. findUnique/update/delete via extendedWhereUnique), injected into create/createMany/upsert-create payloads including nested relation writes. Zero route-query rewrites needed; platform code (login, token verify, rate limit, membership lookup) runs outside the scope on the base client. `requireOrgId()` helper for explicit create payloads; runtime injection is defense-in-depth behind it.
- Middleware: `authRequired` now authenticates AND resolves the tenant (x-kt-org header / kt_org cookie, validated against membership — client-supplied org ids are never trusted; single active membership defaults; multiple → 409 with org list; none → 403; suspended → 403). `authOnly` for /auth/me + /auth/logout. Platform-support staff (`isPlatformSupport`) may enter any org by explicit header; every such request writes a `platform_support.org_access` audit row. Uniform 403 for non-members whether or not the org exists (no enumeration).
- Pure selection logic in `lib/org-resolution.ts` (12 unit tests) + arg-rewriter tests in `lib/org-scope.test.ts` (8 tests).
- Route/service edits: register bootstraps org+membership; invite accept creates membership from invitation.orgId; admin user create + invitation create carry orgId; settings/payroll upserts by orgId; gl-posting sourceKey/account lookups org-scoped; leave-policy/control-account upserts compound; storage keys tenant-prefixed (`<orgId>/employees/...`); seed.ts creates the default org, memberships, and orgId on every row.
- GATE — live adversarial proof **21/21 PASS** (second org + user created platform-level): cross-org LIST empty, READ/UPDATE/DELETE by guessed id → 404, x-kt-org header attacks → 403, guessed org id → 403, trial balance/pay periods/audit invisible, same displayName allowed across orgs (composite unique), own-org CRUD works both directions, /auth/me lists organizations, settings PUT round-trip 200. Full cleanup verified 0 residue.
- Verification: api tsc PASS, workspace typecheck PASS, lint PASS (0 errors), full build PASS, test:api **149/149 PASS** (+12 org-resolution, +8 org-scope).
- Known follow-ups (documented, not blockers): orgId is scalar-only (FK hardening later); roles remain app-global (per-org roles later); org switcher UI deferred (single-org users auto-resolve); admin org-management endpoints deferred; AuditLog auth events with null orgId invisible to org audit viewers by design.
- Next batch: Batch 13 — accounting integrity and period control. See docs/SHIPPING-PLAN.md.
### Batch 13 results (2026-09-20) — accounting integrity + period control (GATE PASSED)

- Fiscal calendar: new `FiscalYear` + `FiscalPeriod` models (calendar-year, periods 1-12, `FiscalPeriodStatus` open/soft_closed/locked, @@unique([orgId,year]) and @@unique([orgId,year,period])); `JournalEntryStatus` gained draft/approved; `DepositLine.accountId` for ad-hoc offset accounts. Migration `20260920190000_fiscal_periods_manual_journals` applied live + resolved; status clean.
- Posting guard: `lib/fiscal-periods.ts assertPeriodOpen(db, date)` runs inside every `postJournal` — soft_closed/locked periods reject with `PeriodClosedError` (409 at the routes); missing periods auto-provision open (calendar-year) so legitimate posting is never blocked by setup. Applies to ALL event postings (invoices, payments, bills, expenses, deposits, payroll accrual/settlement/remittance, reversals).
- Manual journals: routes `GET/POST /finance/journals`, `:id/approve` (draft->approved), `:id/post` (draft|approved->posted, period-guarded), `:id/reverse` (posted-> posts `manual_journal_reversal` + voids original, double-reversal -> 409), `DELETE` draft-only. Period routes: `GET /finance/periods`, `:id/close` (open->soft_closed), `:id/lock` (->locked), `:id/reopen` (platform_owner only). All transitions audit-logged.
- Deposits post through GL: new control account 1150 Undeposited Funds (auto-provisioned by ensureControlAccounts, added to seed); `buildDepositPostedJournal` Dr bank total / Cr UF for payment-linked lines / Cr per-account for ad-hoc lines, posted in the same tx as the deposit; ad-hoc lines REQUIRE an offset account before posting (400 otherwise); void reverses the journal. Deposit create/update UI gained ad-hoc lines with account picker.
- Reports integrity: trial balance + account ledger count posted+void entries (void is always paired with a posted reversal — including both nets to zero and keeps the audit trail; a posted-only filter was tried first and CAUGHT by the live gate as double-reversal, then fixed). NEW `GET /finance/reports/control-reconciliation`: GL control balances (1100 AR, 2000 AP, 2100 NHI, 2200 SSB, 2300 payroll tax, 2500 net wages) vs subledgers (open document balances minus unapplied payments; finalized/exported/paid runs not yet statutory-remitted) with per-row ok/mismatch + allOk.
- Admin UI: new `finance/journals` page (list + balanced draft builder with account pickers + approve/post/reverse/delete), new `finance/periods` page (years->periods with Close/Lock/Reopen), deposits/new ad-hoc section, trial-balance page gained the control-reconciliation card, journal page source labels for manual_journal/deposit_posted/payroll_run_paid/statutory_remittance (+reversals), nav links added.
- SHARED FILE NOTE: `apps/api/src/app.ts` (route mounts) and `apps/admin-web/src/app/dashboard/finance/layout.tsx` were touched — both on the AGENTS.md shared list; no other lane active on them this batch.
- GATE — live proof **30/30 PASS** (`tmp/b13-verify.mjs`, all test rows cleaned, zero residue, owner data untouched): draft->approve->post lifecycle, TB reflects posting, unbalanced journal 400, reversal voids original + nets ledger to zero + double-reversal 409, voided journal undeletable 409, ad-hoc deposit posts Dr 1000/Cr 4000 journal, 1150 auto-provisioned, deposit void posts reversal, soft-close + lock both reject postings with 409 ("fiscal period 2026-09 is closed"), owner reopen works, control reconciliation allOk=true on live data.
- Browser-verified (in-app browser, logged in): Manual journals, Fiscal periods, Trial balance + reconciliation card all render against live API.
- Verification: api tsc PASS, workspace typecheck PASS, lint PASS (1 pre-existing warning, payroll/forms img), full build PASS (admin-web 57 pages), test:api **151/151 PASS** (+2 deposit-builder tests).
- Descoped deliberately (documented): Float->decimal full conversion (JournalLine already Decimal(14,2); document money stays Float with roundMoney at boundaries), journal attachments, dedicated assets/loans/bad-debt flows (manual journals cover them for now).
- Next batch: Batch 14 — banking and reconciliation. See docs/SHIPPING-PLAN.md.
### Batch 14 results (2026-09-20) — banking and reconciliation (GATE PASSED)

- Schema: `BankStatementImport` + `BankStatementLine` (signed amount, sha256 `fingerprint` over bankAccountId|date|amount|description|reference, @@unique([orgId,fingerprint]) so re-imports physically cannot duplicate), `BankReconciliation` (opening/clearedNet/difference, in_progress/completed, audited unlock fields), `BankReconciliationLine` (cleared lines with date/description/amount snapshots for reproducibility). Migration `20260920203000_banking_reconciliation` applied live + resolved; status clean.
- Statement import: `POST /finance/banking/imports/preview|commit` — JSON-body CSV (same transport as QuickBooks import), alias header mapping (date/description/reference/amount or debit/credit pair), currency/date normalization, in-file duplicate warnings, DB duplicate flags in preview, commit skips existing fingerprints (createMany + unique backstop). Max 5000 rows.
- Matching: `lib/bank-matching.ts` pure scoring (amount+sign hard match, date window 7d, reference/description token overlap, 0-100); `GET /banking/lines/:id/suggestions` ranks live documents on the bank account — payments (Dr depositAccount), open deposits, open expenses, bill payments, and posted manual journals (net on account); already-matched documents are excluded. `match` validates amount/sign + document still open + one-document-one-line; `unmatch`/`exclude` guarded when the line is cleared in a completed reconciliation.
- Reconciliation sessions: `POST /banking/reconciliations` (one in-progress per account; opening = previous completed ending), `clear`/`unclear` (recompute clearedNet + difference; excluded lines and lines cleared in completed sessions rejected), `complete` (409 unless difference ~0; marks lines reconciledAt, same tx), `unlock` (reason required, latest completed only, clears reconciledAt, audit-logged) — the controlled undo.
- Bank register: `GET /finance/banking/register/:accountId` — GL ledger running balance annotated with per-row reconciled flags (journal source -> matched entity mapping, reversals included). Reconciliation detail also returns outstanding documents (never-reconciled bank documents).
- Admin UI: `finance/statements` (upload/preview/commit with duplicate badges + line list with match suggestions panel + exclude), `finance/reconciliations` (list + start form) + `[id]` detail (summary cards, clear checkboxes, complete/unlock, outstanding documents), `finance/register` (account picker + running balance table + reconciled ticks); nav links added. Account pickers filtered to Bank/Cash asset subtypes.
- Batch 13 follow-up fixed here: FiscalYear/FiscalPeriod were missing from the db TENANT_MODELS scope list (period queries bypassed org scoping); all four B14 models + both fiscal models added.
- SHARED FILE NOTE: `apps/api/src/app.ts` (route mount) and `apps/admin-web/src/app/dashboard/finance/layout.tsx` (nav) touched — AGENTS.md shared list; no other lane active.
- GATE — live proof **32/32 PASS** (`tmp/b14-verify.mjs`, zero residue): import 4 lines -> re-import creates 0/4 duplicates; suggestions rank the deposit top; match deposit/expense/manual-journal; double-match 409; sign mismatch 409; exclude guards; opening carryover (0 then 75); clearedNet/difference live recompute; complete locks (clear rejected 409); lines marked reconciled; register closing 75 with reconciled flags; unlock requires reason, latest-only, re-completes cleanly.
- Browser-verified (in-app browser, logged in): statements, reconciliations, register pages render against live API.
- Verification: api tsc PASS, workspace typecheck PASS, lint PASS (1 pre-existing warning), full build PASS (admin-web 60 pages), test:api **160/160 PASS** (+9 bank import/matching).
- Next batch: Batch 15 — financial statements + BVI filing support. See docs/SHIPPING-PLAN.md.

### Batch 15 results (2026-09-20) — financial statements + BVI filing support (GATE PASSED)

- Schema: `OrgSettings` filing identity (companyRegistrationNumber, incorporationDate, registeredAgentName, registeredOfficeAddress, fiscalYearEndMonth default 12, filesIrFinancialStatements, annualReturnExemptionBasis); enums `ReportSnapshotType` (profit_loss/balance_sheet/cash_flow/changes_in_equity/aging/payroll_liabilities/annual_return) + `YearEndCloseStatus` (draft/reviewed/approved/posted/superseded); `ReportSnapshot` (immutable payloadJson + sha256 hash over canonical JSON) + `YearEndClose` (preview JSON, closing journal link, review/approve/post + typed signature, superseding-revision self-relation). Migration `20260920210000_statements_year_end_filing` applied live + resolved; status clean. Both models added to db TENANT_MODELS. Equity control accounts 3000 Owner equity / 3100 Retained earnings added to CONTROL_ACCOUNTS + seed (auto-provisioned live at first close posting).
- Statement engine `lib/financial-statements.ts` (pure, unit-tested): P&L (subtype sections), balance sheet (equity = equity accounts + unclosed current earnings; balanceCheck = A−L−E), cash flow (indirect; Bank subtype only is cash, UF is working capital; liability deltas negated for debit-positive sign convention; completenessCheck = cash delta − netChange), changes in equity (consistencyCheck), AR/AP aging buckets (current/1-30/31-60/61-90/90+), canonical JSON + statement hash, CSV renderer. `gl-reports.loadAccountBalances(from?, to?, {excludeSourceTypes, client})` — tx-aware and can exclude closing journals.
- Routes `routes/finance-statements.ts`: GET statements (profit-loss / balance-sheet / cash-flow / equity with from/to/asOf + compare=1 prior-year), aging, payroll-liabilities (GL statutory control accounts + unremitted runs). Locked snapshots: POST/GET/GET:id (intact hash check) + export.csv / export.pdf (pdf-lib; exports render the STORED payload, never a recompute).
- Year-end close: draft → reviewed → approved → posted (typed signature ≥3 chars, one posted close per year), corrections by superseding revision — posting a revision reverses the prior closing journal in the SAME transaction BEFORE recomputing balances (bug found by the gate: the original order computed "nothing to close"; fixed via tx-aware loadAccountBalances). Revise preview shows balances as-if-reversed.
- Closing-journal exclusion: operating P&L (statement + annual-return mapping) excludes `year_end_close` AND `year_end_close_reversal` source types — second gate find: reversals carry the `_reversal` suffix and were double-counting revenue after a revision.
- BVI filing support: GET /finance/filing/annual-return?year — identity from OrgSettings, applicability (2023 Financial Return Order 9-month registered-agent rule + IR exemption note), BS/P&L mapping with balance check, payroll support aggregates by pay date in FY, deadlines, sources, "filing support only" wording throughout — nothing files anything. Settings PUT whitelist gained the 7 filing fields with validation.
- Admin UI: `finance/financial-statements` (4 tabs, date controls, prior-year comparative, lock snapshot + CSV/PDF links + snapshot history), `finance/aging` (bucket cards + AR/AP tables), `finance/year-end` (close list with status badges/signature/revision chain, create form, review→approve→post/revise actions, preview lines), `finance/filing` (identity form + annual-return mapping card + prominent filing-support-only banner + lock snapshot); nav links added to finance layout.
- DESCOPED (per plan): location/job profitability (no schema support — WorkAssignment is scheduling-only), XLSX export (CSV+PDF ship), e-filing (support wording only).
- SHARED FILE NOTE: `apps/api/src/app.ts`, `apps/api/src/routes/settings.ts`, `apps/admin-web/src/app/dashboard/finance/layout.tsx` touched — AGENTS.md shared list; no other lane active.
- GATE — live proof **41/41 PASS** (`tmp/b15-verify.mjs`, zero residue): P&L 150/75/NI 75 with comparative; BS balanceCheck 0 with currentEarnings = NI; CF completenessCheck 0 (net change = cash delta); equity consistencyCheck 0; NI agrees across all three statements; aging/payroll-liability shapes; snapshot hash intact + CSV/PDF exports render locked figures; close draft→review→approve→post (unsigned post 400, duplicate posted year 409); post-close BS at FYE shows RE = NI with currentEarnings 0; operating P&L/CF/equity unaffected by close; revision reverses + supersedes + re-posts balanced; annual-return mapping balanced with operating NI post-revision; filing fields round-trip.
- Browser-verified (in-app browser, logged in): all four new pages render against live API; filing page shows banner, mapping, payroll support (4 active employees), deadlines.
- Verification: api tsc PASS, test:api **168/168 PASS** (+8 statement engine), workspace typecheck PASS, lint PASS (1 pre-existing warning), full build PASS (admin-web 64 pages).
- Next batch: Batch 16 — production delivery (storage/email/WhatsApp/e2e) → Gate B. See docs/SHIPPING-PLAN.md.

### Batch 16 results (2026-09-20) — production delivery and communication (GATE B WORK COMPLETE)

- Storage: real S3/R2 provider in `lib/document-storage.ts` (`@aws-sdk/client-s3` + presigner) — boot-time config validation (fails closed on missing bucket/keys/endpoint), scoped-key enforcement (no absolute/`..` keys), presigned GET URLs (5 min default, clamped 30 s–1 h, computed locally); download routes in people.ts redirect to the signed URL when the provider supports it, else stream through the authenticated API. Local provider unchanged. Org-scoped keys + magic-byte upload validation already in place (B12).
- Email outbox: `EmailMessage` table (migration `20260921100000_email_outbox`; orgId nullable — password-reset mail can precede org context; NOT in TENANT_MODELS, routes scope manually) + rewritten `lib/email.ts`: templates (password_reset, user_invitation), `queueEmail`, worker (`startEmailWorker` in index.ts, 60 s, `EMAIL_WORKER=off` to disable), exponential backoff `2^attempts` min (cap 6 h), 5 attempts then FAILED, at-least-once claim (crash mid-send stays QUEUED, never silently SENT). auth.ts + admin-users.ts now queue instead of sending inline; production invite still 502s when SMTP unconfigured. Admin routes `/admin/email-queue` (list/summary/process/retry, platform_owner operates, hr_admin views own org) + `/admin/email-transport/verify`; UI page `/dashboard/email-queue` + Admin nav entry. `/health/ready` now reports email queue depth + storage provider.
- WhatsApp: `wa.me/<number>?text=` direct links on the tracker-share card when the employee has a phone (`lib/whatsapp.ts`; BVI default: 7-digit → +1-284); generic api.whatsapp.com share remains as fallback. Tracker-share endpoint now returns the phone. Provider sending stays optional (owner credentials).
- E2E: Playwright at repo root (`npm run test:e2e`), 8 specs × desktop/tablet/phone projects — admin login, dashboard, financial statements tabs, year-end, filing support, email queue, people directory, tracker login/home. **24/24 PASS** against live dev stack.
- Ops: `scripts/register-scheduled-backup.ps1` (daily Windows Task, 02:17), `scripts/restore-drill.ps1` (restores latest backup into scratch DB, checks row counts, drops scratch; `-AdminDatabaseUrl` for CREATEDB-capable user; PowerShell-5.1-safe: ASCII only, SQL via temp file), `docs/OPERATIONS.md` (storage/email/WhatsApp/backups/incidents/staging + pilot acceptance checklist). Live drill **PASSED**: backup 20260920-225223 restored (2 users / 7 employees / 1 org).
- GATE B evidence: dependency audit — 0 critical; 4 high all in `mysql2`, a transitive Prisma CLI dev dependency; **not applicable at runtime** (Postgres-only, mysql2 never loaded; `audit fix --force` would downgrade Prisma 7→6, rejected). Tenant isolation: org-scope/org-resolution suites inside 175/175 green. Financial invariants re-run live: b15-verify **41/41 PASS** after B16 changes. Email outbox live gate (`tmp/b16-verify.mjs`) **11/11 PASS**, zero residue: queue row written, flush skips cleanly unconfigured (no silent drop), retry guards, health payload, phone in tracker-share. Browser-verified: email-queue page + employee Access tab showing "WhatsApp Maria directly".
- Remaining for Release B sign-off (owner, not code): run the pilot acceptance checklist in docs/OPERATIONS.md §6 without developer help; supply SMTP + optional S3/R2 and WhatsApp provider credentials.
- Verification: api tsc PASS, test:api **175/175 PASS**, admin-web typecheck PASS, lint PASS (1 pre-existing warning), e2e 24/24.

### Batch 17 results (2026-09-21) — accounting migration safety (GATE 17 COMPLETE)

- Schema (migration `20260921120000_migration_control`): enum `ImportBatchStatus` (11-state lifecycle uploaded→inventoried→mapped→validated→approved→importing→reconciled→accepted, exits rejected/failed/reversed) + models `AccountingImportBatch` / `AccountingImportFile` (sha256, sizeBytes, private storageKey, row/valid/invalid/committed counts) / `AccountingImportRow` (payloadJson, sourceRef idempotency key, status, errorMessage, destinationType+Id lineage) / `ExternalSourceRef` (`@@unique([orgId, sourceSystem, sourceType, sourceId])` — cross-tenant collision impossible) / `MigrationReconciliation` (count/total/trial_balance checks, matched/discrepancy/accepted) / `LegacyDocument`. All six registered in TENANT_MODELS (CP1252-safe patch).
- GL: `openingBalanceEquity` control account 3200 added to CONTROL_ACCOUNTS + ControlAccountKey + seed.ts; `buildOpeningBalanceJournal` posts per-account signed balances against OBE (debit-normal assets/expenses, credit-normal liabilities/equity/revenue), balanced by construction.
- New `lib/migration-import.ts` + `routes/migration-imports.ts` mounted at `/imports/migration`: batch create → file upload (magic-byte sniff, .xlsm/vbaProject rejected, 50 MB, sha256 + document-storage under `<orgId>/migration/…`, reuses B16 storage) → inventory rows → strict validation (unknown customers/vendors/accounts/invoices are INVALID — never silently find-or-created like the legacy importer; GL control accounts such as Undeposited Funds validate by name because ensureControlAccounts provisions them at commit) → approve → **commit in ONE prisma.$transaction**: documents created, journals posted through the Batch 8 engine (invoice/bill/payment/expense/deposit builders), payment applications applied with invoice balance updates, one opening-balance journal per batch, ExternalSourceRef per destination, reconciliation rows, all-or-nothing (any throw = full rollback, batch marked failed) → reverse (counter-journals via reverseJournal, docs deleted, invoice balances restored, lineage cleared, master data removed only when unreferenced, guarded against cross-batch payments/deposit links) → accept. Idempotent: repeat import finds ExternalSourceRef and marks every row `skipped_duplicate` (zero duplicates); postJournal idempotent by sourceKey.
- Import fidelity fixed vs legacy importer: multi-line invoices preserved via `lineAmount` grouping (new alias), payments carry optional `invoiceNumber` alias → real PaymentApplication, deposits require an offset account so they can post, opening balances become real opening AR invoices / AP bills (line account = OBE) + OBE journal instead of notes, account-type parser extended (`loan` → liability).
- Templates + error report: `GET /imports/migration/templates/:type` (canonical CSV headers), `GET /imports/migration/batches/:id/errors.csv` (escaped CSV of invalid rows with payloads).
- UI: `/dashboard/imports/migration` Migration Center (batch create/list, status-gated upload/validate/approve/commit/accept/reverse, files table, reconciliation table, template + error-report downloads) + Admin nav entry; legacy `/dashboard/imports/quickbooks` page now carries an amber "not approved for full migrations" banner pointing at the Migration Center.
- GATE 17 evidence (`tmp/b17-verify.mjs`, adversarial, live): **34/34 PASS**, zero residue — same customers file into org A and org B produces distinct ids with org-scoped ExternalSourceRef keys; customer opening balance posts Dr 1100 AR / Cr 3200 OBE 250.00; multi-line invoice GA-100 keeps both lines (30+25) with a balanced journal; payment GA-PAY-1 creates a real PaymentApplication (20.00), invoice balance 55→35 status partial, balanced payment journal; accounts opening balances post one balanced OBE journal (Dr 1550 savings 500 / Cr 2550 loan 200 / Cr 3200 OBE 300); repeat import commits with zero new customers, rows `skipped_duplicate`; mid-commit failure (Sales account deleted after approve) leaves **zero** invoices/journals/lineage and marks the batch failed; reversal removes payment+applications, restores invoice balance (55, open), posts the counter-journal. Browser-verified: Migration Center page, batch create, detail panel with upload/validate controls.
- Shared files touched (noted under Shared File Locks): `apps/api/src/app.ts` (route mount), `apps/api/src/lib/gl-posting.ts` (OBE account + opening journal builder), `apps/admin-web/src/lib/dashboard-nav.ts` (nav item), `apps/api/src/lib/quickbooks-accounting-import.ts` (exported 3 helpers + added lineAmount/invoiceNumber/offsetAccountName aliases — legacy behavior unchanged).
- Verification: api tsc PASS, test:api **185/185 PASS** (10 new migration-import unit tests), admin-web typecheck PASS, lint PASS (1 pre-existing warning), build PASS, gate 34/34.
- Known scope notes: reversal of master-data-only side effects keeps referenced master rows and reports them in `kept`; commit chunking deferred (all-or-nothing per batch satisfies the gate); ZIP member expansion and QB/Xero source adapters are Batch 18.

### Batch 18 results (2026-09-21) — QuickBooks full coverage + rehearsal (GATE 18 COMPLETE)

- Schema (migration `20260921140000_migration_b18`): `AccountingImportBatch.migrationMode` (full_detail default / cutover) + `exceptionSignature`/`exceptionSignedByUserId`/`exceptionSignedAt`; `AccountingImportFile.disposition` (pending/imported/archived/unsupported) + `dispositionNote`.
- Coverage: every QuickBooks Online export type now has a final disposition. IMPORTABLE and committed through the posting engine: journal_entries (grouped by number, postJournal enforces balance), bill_payments (BillPayment + application + bill balance + journal), sales_receipts (paid invoice + fully-applied payment, both journals), transfers (Dr destination / Cr source), opening_balances (OBE journal). ARCHIVED (hashed + stored + counted, never posted — Batch 19 cost-centre/project targets): estimates, purchase_orders, classes, locations, projects, product_categories, time_activities, attachments (→ `LegacyDocument` with sha256 in private storage). UNSUPPORTED: credit_memos (no credit-note model — surfaces on the exception report). Unknown ZIP members are REFUSED by name: no source record stays unclassified.
- ZIP packages: `inventoryZip` (fflate) expands a QBO-style export in one upload; member names are classified by longest-match type inference (`bill_payments.csv` never lands as `bills`), macro payloads (vbaProject.bin) rejected, 50 MB cap.
- Cutover mode: history types (payments, bill_payments, expenses, deposits, sales_receipts, journal_entries, transfers) are archived with a cutover note instead of posted; customer/vendor openingBalance fields are INVALID (AR/AP opening comes from open documents only — double-count prevention); fully-paid invoices/bills are invalid (open documents only).
- Controls: per-file count checks + batch trial balance + **ar_balance / ap_balance** reconciliation (expected accumulated at document creation, actual from the batch's own Invoice/Bill refs) — a same-batch payment now decrements expectedAr (mirrors bill_payments/expectedAp), so application inside one commit no longer fabricates a discrepancy.
- Exception report + owner sign-off: `GET /imports/migration/batches/:id/exception-report.csv` (every non-imported file with reason + sha256, reconciliation discrepancies, batch line signed/unsigned) and `POST .../sign-exceptions` (typed signature, audit-logged, only after commit; empty signature refused).
- Re-validation fix: rows invalidated by `validateBatch` itself now carry a `destinationType='validation'` marker and are reset at the next validate run — a rejected batch recovers after the org data is fixed (e.g. adding a missing account); inventory-time structural verdicts still require re-uploading the source file.
- UI: Migration Center gained the mode selector (full detail / cutover), ZIP upload (`.csv,.xlsx,.zip`, auto-expand hint), a Disposition badge column (imported/archived/unsupported/pending with reason tooltip), mode + signature in the batch subtitle, Exception report download, and Sign exceptions (typed-signature prompt).
- Rehearsal package: `tmp/b18-rehearsal-package.mjs` builds a realistic 18-member QBO export ZIP (13 importable CSVs, 2 archived, 1 unsupported, 1 attachment) + `tmp/b18-manifest.json` with per-member SHA-256 and control totals (AR 285 / AP 150).
- GATE 18 evidence (`tmp/b18-verify.mjs`, live, 2 orgs): **45/45 PASS**, zero residue — full_detail ZIP commits reconciled with 21/21 reconciliation checks matched (AR 285/285, AP 150/150, TB 560/560); invoice 55→35 partial after payment, bill 80→50 after bill payment, sales receipt = paid invoice + full payment, journal/transfer/opening journals balanced, attachment preserved as hashed LegacyDocument; repeat import = 18/18 rows `skipped_duplicate`, zero new documents; unknown member refused by name; cutover rejects master-data opening balances, archives the payments file (never posted), commits open documents with AR 40 / AP 60 matched; reversal removes every document, posts 13 counter-journals, frees unreferenced master data; cleanup verified zero residue (incl. 43 storage objects).
- Browser-verified full UI lifecycle: batch create → ZIP upload via file input (18 members classified with disposition badges) → validate **rejected** on missing Sales/Checking accounts (strict validation never silently creates) → accounts added → re-validate → **validated** (the re-validation fix working live) → approve → commit → 21/21 matched in the reconciliation table → Sign exceptions ("UI Owner" recorded) → reverse. Smoke residue cleaned to zero.
- Cleanup hardening lesson (also retro-applied): gate cleanup now wipes the org journal tables outright — B17's memo-regex cleanup missed auto-numbered doc journals ("Customer payment GA-PAY-1" etc.); two B17 orphan journals were found and removed during this gate.
- Shared files touched (noted under Shared File Locks): `packages/db/prisma/schema.prisma` (B18 columns), `apps/api/src/app.ts` — untouched this batch; routes/lib changes confined to `apps/api/src/routes/migration-imports.ts` + `apps/api/src/lib/migration-import.ts` + `apps/admin-web/src/app/dashboard/imports/migration/page.tsx`.
- Verification: api tsc PASS, test:api **189/189 PASS**, admin-web typecheck PASS, lint PASS (1 pre-existing warning), build PASS, gate **45/45**, UI lifecycle PASS.
- Known scope notes: payments/bill_payments applied to documents OUTSIDE the batch still net against expected AR/AP (same-batch is the normal rehearsal shape; cross-batch application lands as an explainable discrepancy on the signed exception report); QB/Xero API source adapters (pull instead of upload) remain future work, not shipping blockers.
